<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Modifier l'équipement</title>
</head>
<body>
<h1>Modifier l'équipement {{ equipement.id }}</h1>

<form action="?controller=equipement&action=modifierEquipement" method="post">
    <input type="hidden" name="id" value="{{ equipement.id }}">

    <label for="nom">Nom:</label>
    <input type="text" id="nom" name="nom" value="{{ equipement.nom }}" required>

    <label for="etat">État:</label>
    <input type="text" id="etat" name="etat" value="{{ equipement.etat }}" required>

    <label for="disponible">Disponible:</label>
    <select id="disponible" name="disponible">
        <option value="1" {% if equipement.disponible %}selected{% endif %}>Oui</option>
        <option value="0" {% if not equipement.disponible %}selected{% endif %}>Non</option>
    </select>

    <button type="submit">Modifier</button>
    <a href="?controller=equipement&action=listTousLesEquipements">Retour à la liste</a>
</form>
</body>
</html>
